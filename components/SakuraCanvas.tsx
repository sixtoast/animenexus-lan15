"use client";

/**
 * Sakura petal layer — removed from product surface (Sprint 35).
 * Component kept as a no-op so any residual imports do not break the build.
 */
export function SakuraCanvas() {
  return null;
}
