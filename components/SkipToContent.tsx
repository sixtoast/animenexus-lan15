/** Skip link — first focusable control (Sprint 34). Server-safe. */

export function SkipToContent() {
  return (
    <a href="#main-content" className="skip-link">
      Skip to content
    </a>
  );
}
