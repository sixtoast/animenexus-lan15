# Companion model drop-in

Place a GLB here as **`companion.glb`** (Y-up, roughly 1 unit tall).

Until the file exists, Lantern-ko uses the procedural chibi.

Recommended:
- Idle / walk clips named clearly if you later wire `useAnimations`
- Low poly for mobile
- Neutral T-pose or idle facing +Z
